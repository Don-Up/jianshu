# Task 2: Comments Module - Nested Replies & Likes

## 第一部分: 概述

扩展 Comments 模块支持无限嵌套回复和评论点赞功能。Service 层新增 `likeComment`/`unlikeComment` 方法实现点赞功能；`findByArticle` 改用 slug 查询并返回嵌套树结构；`delete` 方法增加软删除逻辑（有子回复时保留结构）；DTO 增加 `parentId` 字段支持回复。

---

## 第二部分: 改动文件清单

### 修改文件

| 文件路径 | 功能类别 | 说明 |
|---------|---------|------|
| `apps/api/src/comments/comments.service.ts` | Service | 新增点赞、嵌套树构建、软删除逻辑 |
| `apps/api/src/comments/dto/create-comment.dto.ts` | DTO | 增加 parentId 字段 |

### 文件结构

```
apps/api/src/comments/
├── comments.service.ts (修改)
└── dto/
    └── create-comment.dto.ts (修改)
```

---

## 第三部分: 详细改动说明

### create-comment.dto.ts[修改](apps/api/src/comments/dto/create-comment.dto.ts)

```typescript
import { IsNotEmpty, IsString, MaxLength, IsOptional } from 'class-validator';

export class CreateCommentDto {
  @IsString()
  @IsNotEmpty({ message: '评论内容不能为空' })
  @MaxLength(2000, { message: '评论最多2000字' })
  content!: string;

  @IsOptional()
  @IsString()
  parentId?: string; // for nested replies
}
```

**说明：** 新增 `parentId` 可选字段，创建回复时传入父评论ID。

---

### comments.service.ts[修改](apps/api/src/comments/comments.service.ts)

**1. buildCommentTree - 嵌套评论树构建**

```typescript
private buildCommentTree(comments: any[], userId?: string): CommentNode[] {
  const map = new Map<string, CommentNode>();
  const roots: CommentNode[] = [];

  // First pass: create all nodes
  for (const comment of comments) {
    const isLiked = userId ? comment.likes?.some((l: any) => l.userId === userId) : false;
    const node: CommentNode = {
      id: comment.id,
      content: comment.content,
      authorId: comment.authorId,
      author: comment.author,
      likeCount: comment._count?.likes || 0,
      isLiked,
      createdAt: comment.createdAt,
      parentId: comment.parentId,
      replies: [],
    };
    map.set(comment.id, node);
  }

  // Second pass: build tree
  for (const comment of comments) {
    const node = map.get(comment.id)!;
    if (comment.parentId && map.has(comment.parentId)) {
      map.get(comment.parentId)!.replies.push(node);
    } else if (!comment.parentId) {
      roots.push(node);
    }
  }

  return roots;
}
```

**说明：** 两遍遍历构建嵌套结构。第一遍创建所有节点，第二遍将子节点挂到父节点的 replies 下。顶层评论（parentId=null）作为根节点返回。

---

**2. likeComment - 点赞评论**

```typescript
async likeComment(commentId: string, userId: string) {
  const comment = await this.prisma.comment.findUnique({
    where: { id: commentId },
  });

  if (!comment) {
    throw new NotFoundException('评论不存在');
  }

  const existingLike = await this.prisma.commentLike.findUnique({
    where: { commentId_userId: { commentId, userId } },
  });

  if (existingLike) {
    throw new BadRequestException('已经点赞过该评论');
  }

  await this.prisma.commentLike.create({ data: { commentId, userId } });

  const likeCount = await this.prisma.commentLike.count({ where: { commentId } });

  return { success: true, data: { likeCount } };
}
```

**说明：** 通过 unique 约束检测重复点赞，创建成功后返回最新点赞数。

---

**3. unlikeComment - 取消点赞**

```typescript
async unlikeComment(commentId: string, userId: string) {
  const existingLike = await this.prisma.commentLike.findUnique({
    where: { commentId_userId: { commentId, userId } },
  });

  if (!existingLike) {
    throw new BadRequestException('还未点赞该评论');
  }

  await this.prisma.commentLike.delete({
    where: { commentId_userId: { commentId, userId } },
  });

  const likeCount = await this.prisma.commentLike.count({ where: { commentId } });

  return { success: true, data: { likeCount } };
}
```

---

**4. delete - 软删除逻辑**

```typescript
// If has replies, soft delete by clearing content instead of hard delete
if (comment._count.replies > 0) {
  await this.prisma.comment.update({
    where: { id: commentId },
    data: { content: '[已删除]' },
  });
} else {
  await this.prisma.comment.delete({ where: { id: commentId } });
  if (!comment.parentId) {
    await this.prisma.article.update({
      where: { id: comment.articleId },
      data: { commentCount: { decrement: 1 } },
    });
  }
}
```

**说明：** 有子回复的评论仅清空内容保留结构，无回复的评论才真正删除并递减计数。

---

## 第四部分: 测试方法

### 前提条件
- 后端服务运行中：`pnpm --filter @jianshu/api dev`
- 已登录用户 token

### 测试 1: 创建嵌套回复

**操作步骤：**
1. 对某篇文章发送 POST `/api/articles/:slug/comments`，body: `{"content": "顶层评论"}`
2. 获取返回的 comment id
3. 再次发送 POST `/api/articles/:slug/comments`，body: `{"content": "回复", "parentId": "<上一步的id>"}`

**预期结果：**
- 都能创建成功
- GET 评论列表时返回嵌套结构

---

### 测试 2: 点赞/取消点赞

**操作步骤：**
1. 发送 POST `/api/comments/:commentId/like`
2. 再次发送同一请求

**预期结果：**
- 第一次返回成功
- 第二次返回 "已经点赞过该评论"
- DELETE 请求可取消点赞

---

### 测试 3: 软删除

**操作步骤：**
1. 创建一条评论
2. 对该评论创建一条回复
3. 删除顶层评论

**预期结果：**
- 顶层评论内容变为 `[已删除]`
- 回复仍保留

---

## 其它

### 设计亮点

- **嵌套树两遍遍历**：第一遍创建所有节点并建立 Map 索引，第二遍快速构建树结构，时间复杂度 O(n)
- **数据库唯一约束防重复点赞**：利用 `@@unique([commentId, userId])` 在数据库层保证数据一致性
- **软删除保留回复链**：有子回复的评论仅清空内容，避免删除后回复变成孤儿

### 注意事项

- Controller 层暂未修改（仍使用 articleId 路径参数），后续需配合前端调整
- 点赞数不计入 Article.commentCount，仅 CommentLike 表独立管理
