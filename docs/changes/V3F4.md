# V3 Frontend Implementation Plan - Task 4
## Functional User Profile Follow Button

**Date:** 2026-06-18
**Status:** ✅ Completed

---

## 1. Overview

为用户资料页的关注按钮添加 Optimistic Update（乐观更新）和错误回滚功能。使用 TanStack Query 的 `useMutation` 实现更可靠的关注/取消关注操作。

---

## 2. Files Changed

### Modified Files

| File | Change Type | Description |
|------|-------------|-------------|
| `apps/web/src/components/user/profile-header.tsx` | Modified | 重构为使用 TanStack Query useMutation |
| `apps/web/src/components/user/__tests__/profile-header.test.tsx` | Modified | 新增 Optimistic Update 和 Rollback 测试 |

---

## 3. Implementation Details

### 3.1 ProfileHeader Component Changes

**File:** `apps/web/src/components/user/profile-header.tsx`

#### Before (Old Implementation)

```typescript
const [isFollowing, setIsFollowing] = useState(initialIsFollowing || false);
const [isFollowingLoading, setIsFollowingLoading] = useState(false);

const handleFollow = async () => {
  setIsFollowingLoading(true);
  try {
    const res = await userApi.follow(user.id);
    if (res.success && res.data) {
      setIsFollowing(res.data.isFollowing);
    }
  } catch (error) {
    console.error('Failed to follow/unfollow:', error);
  } finally {
    setIsFollowingLoading(false);
  }
};
```

#### After (New Implementation)

```typescript
import { useState } from 'react';
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';

const [optimisticIsFollowing, setOptimisticIsFollowing] = useState(initialIsFollowing || false);
const queryClient = useQueryClient();

const followMutation = useMutation({
  mutationFn: () => userApi.follow(user.id),
  onMutate: async () => {
    // Optimistic update: immediately update UI
    setOptimisticIsFollowing(!optimisticIsFollowing);
  },
  onSuccess: (res) => {
    if (res.success && res.data) {
      setOptimisticIsFollowing(res.data.isFollowing);
      // Invalidate user profile cache
      queryClient.invalidateQueries({ queryKey: ['users', user.username] });
      toast.success(res.data.isFollowing ? '关注成功' : '已取消关注');
    }
  },
  onError: () => {
    // Rollback on error
    setOptimisticIsFollowing(!optimisticIsFollowing);
    toast.error('操作失败，请重试');
  },
});
```

#### Button Changes

```tsx
<Button
  variant={optimisticIsFollowing ? 'secondary' : 'default'}
  size="sm"
  onClick={() => followMutation.mutate()}
  disabled={followMutation.isPending}
>
  {followMutation.isPending ? '处理中...' : optimisticIsFollowing ? '已关注' : '关注'}
</Button>
```

### 3.2 Test Additions

**File:** `apps/web/src/components/user/__tests__/profile-header.test.tsx`

新增测试用例：

| Test Case | Description |
|-----------|-------------|
| `should update UI immediately without waiting for API response` | 验证乐观更新：点击后立即显示"已关注" |
| `should call API when follow button is clicked` | 验证 API 被正确调用 |
| `should rollback to previous state when API fails` | 验证错误回滚：API 失败时恢复原状态 |

---

## 4. Key Features

### 4.1 Optimistic Update

点击关注按钮后，UI 会**立即更新**（从"关注"变为"已关注"），无需等待 API 响应。这提供更好的用户体验。

**流程：**
1. 用户点击"关注"按钮
2. `onMutate` 回调立即执行，切换 `optimisticIsFollowing` 状态
3. UI 立即显示"已关注"（即使 API 还在请求中）
4. API 响应后，`onSuccess` 更新最终状态并显示 toast

### 4.2 Cache Invalidation

关注状态改变后，调用 `queryClient.invalidateQueries` 使用户资料缓存失效，确保下次访问时刷新数据。

```typescript
queryClient.invalidateQueries({ queryKey: ['users', user.username] });
```

### 4.3 Error Rollback

如果 API 请求失败，`onError` 回调会将状态回滚到操作前的状态，确保 UI 与实际状态一致。

```typescript
onError: () => {
  setOptimisticIsFollowing(!optimisticIsFollowing);
  toast.error('操作失败，请重试');
};
```

### 4.4 Toast Notifications

- 成功时显示"关注成功"或"已取消关注"
- 失败时显示"操作失败，请重试"

---

## 5. Verification

### Tests

| Test Suite | Result | Details |
|------------|--------|---------|
| `profile-header.test.tsx` | ✅ PASS | 16/16 tests passed |

**新增测试覆盖：**
- Optimistic update 行为
- API 调用验证
- 错误回滚机制

### Build

| Command | Result |
|---------|--------|
| `pnpm --filter @jianshu/web build` | ✅ PASS |

---

## 6. Technical Notes

### TanStack Query Mutation Lifecycle

```
User Click
    ↓
onMutate (Optimistic Update) → UI Updates Immediately
    ↓
mutationFn (API Request)
    ↓
├── Success → onSuccess → Cache Invalidation + Toast
│
└── Error → onError → Rollback + Toast
```

### State Management

| State | Purpose |
|-------|---------|
| `optimisticIsFollowing` | 追踪乐观更新后的关注状态 |
| `followMutation.isPending` | 控制按钮禁用状态和加载文本 |

---

## 7. Related Plan Items

- **Task 3** (已完成): Unread Notification Badge in Header
- **Task 4** (本任务): Functional User Profile Follow Button ← 已完成
- **Task 5**: Image Upload in Article Editor

---

## 8. Dependencies

使用已有的 TanStack Query，无需新增依赖：

```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query';
```
