# V3 Frontend Implementation Plan - Task 3
## Unread Notification Badge in Header

**Date:** 2026-06-18
**Status:** ✅ Completed

---

## 1. Overview

在 Header 组件中添加未读通知计数 badge，当用户有未读通知时显示红色数字 badge，链接到通知页面。

---

## 2. Files Changed

### Modified Files

| File | Change Type | Description |
|------|-------------|-------------|
| `apps/web/src/components/layout/header.tsx` | Modified | 添加通知图标和未读计数 badge |
| `apps/web/src/components/layout/__tests__/header.test.tsx` | Created | 新增 Header 组件测试 |

### New Files

| File | Description |
|------|-------------|
| `changes/V3F3.md` | 本变更报告 |

---

## 3. Implementation Details

### 3.1 Header Component Changes

**File:** `apps/web/src/components/layout/header.tsx`

#### Added Imports

```typescript
import { useNotifications } from '@/hooks/use-notifications';
```

#### Added Hook Usage

```typescript
const { unreadCount } = useNotifications();
```

#### Added Notification Badge UI

在 SearchInput 和 "写文章" 按钮之间添加了通知图标和 badge：

```tsx
<Link
  href="/notifications"
  className="relative p-2 text-muted-foreground hover:text-foreground"
>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    className="h-5 w-5"
    fill="none"
    viewBox="0 0 24 24"
    stroke="currentColor"
    strokeWidth={2}
  >
    <path
      strokeLinecap="round"
      strokeLinejoin="round"
      d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"
    />
  </svg>
  {unreadCount > 0 && (
    <span className="absolute -top-1 -right-1 flex h-5 w-5 items-center justify-center rounded-full bg-destructive text-xs text-destructive-foreground">
      {unreadCount > 99 ? '99+' : unreadCount}
    </span>
  )}
</Link>
```

**功能说明：**
- 使用 SVG 铃铛图标作为通知入口
- `absolute -top-1 -right-1` 定位 badge 在图标右上角
- `bg-destructive` 使用主题的破坏性颜色（红色）
- 当 `unreadCount > 99` 时显示 "99+"
- 点击跳转到 `/notifications` 页面

### 3.2 Header Component Tests

**File:** `apps/web/src/components/layout/__tests__/header.test.tsx`

新增 5 个测试用例：

| Test Case | Description |
|-----------|-------------|
| `should show notification bell icon` | 验证通知铃铛图标存在 |
| `should link to /notifications page` | 验证点击跳转到通知页面 |
| `should show unread count badge when unreadCount > 0` | 验证未读数大于0时显示 badge |
| `should show badge with destructive color when unreadCount > 0` | 验证 badge 使用破坏性颜色 |
| `should not show badge when unreadCount is 0` | 验证未读数为0时不显示 badge |

---

## 4. Dependencies

使用已存在的 `useNotifications` hook，无需新增依赖。

**Hook 位置：** `apps/web/src/hooks/use-notifications.ts`

**Hook 提供的数据：**
```typescript
{
  notifications: Notification[],
  total: number,
  unreadCount: number,      // ⬅️ 用于显示未读计数
  isLoading: boolean,
  markAsRead: (id: string) => void,
  markAllAsRead: () => void,
}
```

---

## 5. Verification

### Tests

| Test Suite | Result | Details |
|------------|--------|---------|
| `header.test.tsx` | ✅ PASS | 5/5 tests passed |

### Build

| Command | Result |
|---------|--------|
| `pnpm --filter @jianshu/web build` | ✅ PASS |

---

## 6. UI Reference

### Badge Appearance

```
┌─────────────────────────────────────────────────┐
│  [🔍]  [🔔5]  [✏️ 写文章]  [头像 ▼]            │
│         ↑                                       │
│      Notification Badge                         │
└─────────────────────────────────────────────────┘
```

### Badge States

| State | Appearance |
|-------|------------|
| `unreadCount = 0` | 不显示 badge |
| `unreadCount = 1-99` | 显示数字，如 "5" |
| `unreadCount >= 100` | 显示 "99+" |

---

## 7. Related Plan Items

- **Task 2** (已验证完成): Add Search Dropdown to Header
- **Task 3** (本任务): Unread Notification Badge in Header ← 已完成
- **Task 4**: Functional User Profile Follow Button

---

## 8. Notes

1. 通知图标仅在用户登录后显示（因为 `useNotifications` 需要认证 token）
2. badge 使用 `bg-destructive` 颜色，与其他破坏性操作保持一致
3. 铃铛图标使用 Tailwind CSS 的 `text-muted-foreground` 颜色，与其他图标保持一致
4. 完整的通知列表页面 `/notifications` 已存在
