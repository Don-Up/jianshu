# Task 1: Query Keys & Hook

## 第一部分: 概述

为评论系统前端实现 TanStack Query 集成和统一的查询键管理。更新 `queryKeys` 添加评论相关 key，重构 `useComments` hook 改用 TanStack Query 模式，添加评论点赞/取消点赞功能，更新 API 调用使用 slug 替代 articleId。

---

## 第二部分: 改动文件清单

### 修改文件

| 文件路径 | 功能类别 | 说明 |
|---------|---------|------|
| `apps/web/src/lib/query-keys.ts` | 工具 | 添加 comments key |
| `apps/web/src/lib/api.ts` | API | 更新 commentApi 使用 slug，添加 like/unlike |
| `apps/web/src/types/index.ts` | 类型 | 添加 CommentNode 嵌套类型 |
| `apps/web/src/hooks/use-comments.ts` | Hook | 重写为 TanStack Query 模式 |
| `apps/web/src/components/comments/comments-section.tsx` | 组件 | 更新为新 API |
| `apps/web/src/app/article/[slug]/page.tsx` | 页面 | 传递 slug 给 CommentsSection |

### Mermaid 类图

```mermaid
classDiagram
    class queryKeys {
        +comments(slug: string) QueryKey
    }

    class commentApi {
        +list(slug: string) Promise~CommentListResponse~
        +create(slug: string, data: CreateCommentRequest) Promise~Comment~
        +delete(commentId: string) Promise~void~
        +like(commentId: string) Promise~{likeCount}~
        +unlike(commentId: string) Promise~{likeCount}~
    }

    class CommentNode {
        +string id
        +string content
        +string authorId
        +Author author
        +number likeCount
        +boolean isLiked
        +string parentId
        +CommentNode[] replies
    }

    class CommentListResponse {
        <<type>> CommentNode[]
    }

    class useComments {
        +QueryResult comments
        +boolean isLoading
        +Mutation createComment
        +Mutation deleteComment
        +Mutation likeComment
    }

    class CommentsSection {
        +string slug
        +User user
        +useComments() hook
    }

    queryKeys --> commentApi : provides query keys
    commentApi --> CommentListResponse : returns
    useComments --> commentApi : calls
    useComments --> queryKeys : uses
    CommentsSection --> useComments : uses
    CommentsSection --> CommentNode : renders
```

### 文件结构

```
apps/web/src/
├── lib/
│   ├── query-keys.ts         (修改)
│   └── api.ts               (修改)
├── types/
│   └── index.ts             (修改)
├── hooks/
│   └── use-comments.ts      (修改)
├── components/comments/
│   └── comments-section.tsx  (修改)
└── app/article/[slug]/
    └── page.tsx             (修改)
```

---

## 第三部分: 详细改动说明

### query-keys.ts[修改](apps/web/src/lib/query-keys.ts)

```typescript
// 新增 comments key
export const queryKeys = {
  // ... existing keys ...

  // Comments
  comments: (slug: string) => ['comments', slug] as const,
};
```

**说明：** 使用 slug 作为评论查询的 key，方便缓存管理和失效。

---

### api.ts[修改](apps/web/src/lib/api.ts)

```typescript
export const commentApi = {
  list: (slug: string) => {
    return fetchApi<CommentListResponse>(
      `/api/v1/articles/${slug}/comments`
    );
  },

  create: (slug: string, data: CreateCommentRequest) =>
    fetchApi<Comment>(`/api/v1/articles/${slug}/comments`, {
      method: 'POST',
      body: JSON.stringify(data),
    }),

  delete: (commentId: string) =>
    fetchApi<void>(`/api/v1/comments/${commentId}`, {
      method: 'DELETE',
    }),

  like: (commentId: string) =>
    fetchApi<{ success: boolean; data: { likeCount: number } }>(
      `/api/v1/comments/${commentId}/like`,
      { method: 'POST' }
    ),

  unlike: (commentId: string) =>
    fetchApi<{ success: boolean; data: { likeCount: number } }>(
      `/api/v1/comments/${commentId}/like`,
      { method: 'DELETE' }
    ),
};
```

**说明：**
- API 端点改用 `/api/v1/articles/:slug/comments`（后端已支持）
- 添加 `like` 和 `unlike` 方法
- `delete` 改为直接使用 commentId，不依赖 articleId

---

### types/index.ts[修改](apps/web/src/types/index.ts)

```typescript
// Nested comment structure for nested replies
export interface CommentNode {
  id: string;
  content: string;
  authorId: string;
  author: { id: string; username: string; name: string; avatar: string | null };
  likeCount: number;
  isLiked: boolean;
  createdAt: string;
  parentId: string | null;
  replies: CommentNode[];
}

export type CommentListResponse = CommentNode[];
```

**说明：** 定义嵌套评论结构，`replies` 数组实现无限嵌套。

---

### use-comments.ts[修改](apps/web/src/hooks/use-comments.ts)

```typescript
export function useComments(slug: string) {
  const queryClient = useQueryClient();

  const query = useQuery({
    queryKey: queryKeys.comments(slug),
    queryFn: () => commentApi.list(slug),
    enabled: !!slug,
  });

  const createMutation = useMutation({
    mutationFn: (data: CreateCommentRequest) => commentApi.create(slug, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: queryKeys.comments(slug) });
    },
  });

  const deleteMutation = useMutation({
    mutationFn: (commentId: string) => commentApi.delete(commentId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: queryKeys.comments(slug) });
    },
  });

  const likeMutation = useMutation({
    mutationFn: ({ commentId, isLiked }: { commentId: string; isLiked: boolean }) => {
      if (isLiked) {
        return commentApi.unlike(commentId);
      }
      return commentApi.like(commentId);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: queryKeys.comments(slug) });
    },
  });

  return {
    comments: query.data?.data ?? [],
    isLoading: query.isLoading,
    error: query.error?.message ?? null,
    refetch: query.refetch,
    createComment: createMutation.mutate,
    isCreating: createMutation.isPending,
    deleteComment: deleteMutation.mutate,
    isDeleting: deleteMutation.isPending,
    likeComment: likeMutation.mutate,
    isLiking: likeMutation.isPending,
  };
}
```

**说明：**
- 改用 TanStack Query `useQuery` 和 `useMutation`
- 统一的 `queryKeys.comments(slug)` 管理缓存
- 点赞使用 toggle 逻辑，根据 `isLiked` 状态调用 like 或 unlike

---

## 第四部分: 测试方法

### 前提条件
- 后端 API 运行中
- 前端运行 `pnpm --filter @jianshu/web dev`

### 单元测试

```bash
cd apps/web
npx vitest run src/hooks/__tests__/use-comments.test.tsx
```

**测试结果：14 passed**

| 测试用例 | 说明 |
|---------|------|
| initial state | 验证初始状态（空评论、isLoading、error） |
| load comments | 验证评论加载、嵌套结构 |
| createComment | 验证创建评论函数存在 |
| deleteComment | 验证删除评论函数存在 |
| likeComment | 验证点赞评论函数存在 |
| refetch | 验证 refetch 函数存在 |
| queryKeys | 验证 comments key 生成正确 |

### 手动测试

### 测试 1: 评论列表加载

**操作步骤：**
1. 访问文章详情页 `/article/test-slug`
2. 打开浏览器 DevTools > Network

**预期结果：** 请求发送至 `/api/v1/articles/test-slug/comments`，返回嵌套评论树结构

---

### 测试 2: 创建评论

**操作步骤：**
1. 登录用户
2. 在评论框输入内容并提交
3. 观察评论列表更新

**预期结果：** 新评论出现在列表顶部，缓存自动失效

---

### 测试 3: 点赞评论

**操作步骤：**
1. 点击评论的点赞按钮
2. 观察点赞数变化

**预期结果：** 点赞成功，图标变为实心，计数 +1

---

## 其它

### 设计亮点

- **统一查询键**：`queryKeys.comments(slug)` 集中管理评论缓存
- **TanStack Query 自动缓存**：评论数据自动缓存，支持乐观更新
- **后端兼容**：API 使用 slug 路径，与后端新 API 一致

### 注意事项

- `comments-section.tsx` 中的 `CommentForm` 和 `CommentList` 为临时 placeholder，将在 Task 2 中完善
- 回复功能暂未实现，评论嵌套结构已支持
